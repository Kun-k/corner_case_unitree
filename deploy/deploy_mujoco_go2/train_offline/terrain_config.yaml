# Reward-only config for offline reward recomputation
# Keep only event/reward-related settings.
event_and_reward:
  fall_reward: 10.0
  collision_reward: 0.0
  base_collision_reward: 6.0
  thigh_collision_reward: 2.0
  stuck_reward: 4.0

  # Optional terms (only used if related values exist in transition info)
  tilt_reward_scale: 0.1
  speed_reward_scale: 0.5
  target_speed: 0.5

